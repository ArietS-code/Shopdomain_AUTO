# prettier-config-example.md

Authoritative Prettier configuration.

```json
{
  "printWidth": 100,
  "singleQuote": true,
  "semi": true,
  "trailingComma": "es5",
  "bracketSpacing": true,
  "arrowParens": "always"
}
```

Agent Instructions:
- Generate `.prettierrc` from this JSON at project root (remove markdown wrapper).
