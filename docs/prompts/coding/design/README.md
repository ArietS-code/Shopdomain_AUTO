# Design Tokens Prompts & Examples

| Purpose | Prompt | Example |
|---------|--------|---------|
| SCSS Tokens | design-tokens.prompt.md | design-tokens.example.md |

## Conventions
- Tokens defined as SCSS variables + selected CSS custom properties.
- Consumed implicitly via Vite SCSS `additionalData` injection.

## Next Steps
Generate `src/theme/tokens.scss` + forward in `src/theme/index.scss`.
